#include<stdio.h>
int area(float m,float n)
{
return (1/2)*m*n;
}
main()
{
 float base,height;
 int answer;
 printf("enter the base and height of a triangle\n");
 scanf("%f%f",&base,&height);
 answer=area(base,height);
 printf("area is %d",answer);
}
 
